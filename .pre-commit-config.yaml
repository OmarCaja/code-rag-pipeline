repos:
  - repo: local
    hooks:
      - id: mypy
        name: mypy
        description: "This hook triggers mypy for checking types at src/ level."
        entry: uv run mypy src/
        language: system
        types: [python]
        pass_filenames: false